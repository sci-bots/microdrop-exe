etc/microdrop/plugins/enabled/mr_box_plugin/__init__.py
@@ -20,6 +20,8 @@ from pygtkhelpers.ui.extra_dialogs import yesno, FormViewDialog
 from pygtkhelpers.ui.objectlist import PropertyMapper
 from pygtkhelpers.utils import dict_to_form
 from scipy.optimize import curve_fit
+import base_node_rpc as bnr
+import base_node_rpc.async
 import gobject
 import gtk
 import microdrop_utility as utility
@@ -903,6 +905,17 @@ class MrBoxPeripheralBoardPlugin(AppDataController, StepOptionsController,
                 pass

             try:
+                loop = bnr.async.ensure_event_loop()
+                df_comports = loop.run_until_complete(
+                    bnr.async._available_devices(timeout=.1)
+                )
+                if not len(df_comports):
+                    # No compatible devices found on serial ports.
+                    time.sleep(1)
+                    continue
+                else:
+                    print(df_comports)
+
                 self.board = mrbox.SerialProxy(baudrate=57600, settling_time_s=2.5)

                 host_software_version = utility.Version.fromstring(
